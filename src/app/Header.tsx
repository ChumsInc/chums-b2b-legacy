import React from 'react';
import NavBarUI from "../ducks/menu/components/NavBarUI";

const Header = () => {
    return (
        <header>
            <NavBarUI/>
        </header>
    );
};

export default Header;
